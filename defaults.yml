---



# Begin Linux system user vars

web_group: "www-data"

# Begin database vars


app_db_name: "{{ app }}"
app_db_host: "localhost"
app_path: "/var/www/vhosts/{{ app }}"

my_pub_ip4: "{{ ansible_default_ipv4.address }}"
my_pub_ip6: "{{ ansible_default_ipv6.address }}"